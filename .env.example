# Layered-memory integration test endpoints (matches docker-compose.yml).
# These are TEST variables: the gateway runtime reads BCO_GATEWAY_* instead.
BCO_TEST_POSTGRES_URL=postgres://bco:bco@127.0.0.1:5432/bco
BCO_TEST_REDIS_URL=redis://127.0.0.1:6379/0
